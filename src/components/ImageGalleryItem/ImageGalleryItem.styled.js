import styled from 'styled-components';

export const Image = styled.img`
  height: 200px;
  width: 100%;
  object-fit: cover;
  overflow: hidden;
`;

export const ImageWrap = styled.div`
  cursor: pointer;
`;
